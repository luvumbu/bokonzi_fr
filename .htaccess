# Bloquer l'acces HTTP aux fichiers sensibles
<FilesMatch "^(\.credentials|\.google_oauth)\.php$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</FilesMatch>
